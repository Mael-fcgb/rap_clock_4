<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body text-center">
                        <h2 class="card-title"><%= artist.pseudo %></h2>
                        <img src="<%= artist.image %>" alt="<%= artist.pseudo %>" class="img-fluid mb-3">
                        <p><strong>Description:</strong> <%= artist.description %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
